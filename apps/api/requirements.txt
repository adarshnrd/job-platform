# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
httpx>=0.27.0

# Database
# 2.7.4 predates Supabase's new sb_secret_/sb_publishable_ API key format —
# its client rejects those keys with "Invalid API key" even though the
# server accepts them. 2.31.0+ fixes this; postgrest is pulled in
# transitively at a matching version, so it's not pinned separately.
supabase>=2.31.0

# Scheduling (replaces Celery/Redis)
apscheduler>=3.10.4

# Browser automation
playwright>=1.47.0

# Web scraping
beautifulsoup4>=4.12.3
lxml>=5.3.0

# Document processing
python-docx>=1.1.2
pypdf2>=3.0.1
openpyxl>=3.1.5

# Email
resend>=2.3.0

# Auth
PyJWT>=2.9.0

# Utilities
pydantic>=2.11.7,<3.0.0
pydantic-settings>=2.6,<3
python-dotenv>=1.0.1
tenacity>=9.0.0
loguru>=0.7.2
cryptography>=43.0.1
